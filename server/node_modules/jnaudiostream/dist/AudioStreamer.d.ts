import MediaBuffer from "./MediaBuffer";
export declare class AudioStreamer {
    chunksDuration: number;
    chunksSeconds: number;
    debug: boolean;
    playing: boolean;
    latency: number;
    mimeType?: string;
    onStop?: () => void;
    audioContext: AudioContext;
    audioElement: HTMLAudioElement;
    mediaBuffer?: ReturnType<typeof MediaBuffer>;
    constructor(chunksDuration?: number);
    stop(): void;
    setBufferHeader(packet: {
        mimeType: string;
        data: ArrayBuffer;
        startTime: number;
    }): void;
    playStream(): void;
    receiveBuffer(packet: [ArrayBuffer, number]): void;
}
