declare const MediaBuffer: (bufferHeader: BufferSource, mimeType: string, chunksDuration?: number) => {
    objectURL: string;
    append: (arrayBuffer: ArrayBuffer) => number | false;
    stop: () => void;
};
export default MediaBuffer;
